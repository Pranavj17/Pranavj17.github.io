title: Pranav J — Engineering Notes
description: Production AI infrastructure, LLM benchmarks, observability engineering.
author: Pranav J
email: pranav.j@scripbox.com
url: "https://pranavj17.github.io"
baseurl: ""

theme: minima
markdown: kramdown

# Disable Rouge syntax highlighting wrappers — keep code blocks as plain
# <pre><code class="language-X">...</code></pre> so Medium's import handles them cleanly.
# `syntax_highlighter_opts.disable: true` is the documented way to force-disable Rouge.
kramdown:
  input: GFM
  hard_wrap: false
  syntax_highlighter_opts:
    disable: true

permalink: /:year/:month/:day/:title/

minima:
  social_links:
    github: Pranavj17
    linkedin: pranav-j

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "Pranav J"

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - .gitignore
